# Amazon ListingLoader Template

Posizionare qui il file ufficiale Amazon Seller Central **ListingLoader.xlsm**.

Il file deve chiamarsi esattamente `ListingLoader.xlsm` e deve essere il template XLSM originale con macro VBA.

Una volta posizionato, sarà servito automaticamente all'URL `/amazon/ListingLoader.xlsm`.
